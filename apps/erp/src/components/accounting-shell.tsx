import Link from "next/link";
import { BarChart3, BookOpen, Building2, CreditCard, FileText, Landmark, ReceiptText, Settings, Upload, WalletCards } from "lucide-react";

const links = [["Overview", "/accounts", BarChart3], ["Bank reconciliation", "/accounts/bank-reconciliation", Landmark], ["Statement importer", "/accounts/statement-imports", Upload], ["Bank accounts", "/accounts/bank-accounts", WalletCards], ["Banks", "/accounts/banks", Building2], ["Bank transactions", "/accounts/bank-transactions", BarChart3], ["Matching rules", "/accounts/bank-transaction-rules", Settings], ["Accounting settings", "/accounts/settings", Settings], ["Cost centers", "/accounts/cost-centers", BookOpen], ["Journal entries", "/accounts/journal-entries", FileText], ["Invoices", "/accounts/purchase-invoices", ReceiptText], ["Payments", "/accounts/payment-entries", CreditCard], ["Reports", "/accounts/reports", Landmark]] as const;

export function AccountingShell({ children }: Readonly<{ children: React.ReactNode }>) {
  return <div className="flex min-h-screen bg-muted/30"><aside className="hidden w-64 shrink-0 border-r bg-slate-950 text-slate-300 md:block"><div className="flex h-16 items-center gap-3 border-b border-slate-800 px-5 text-sm font-bold tracking-[0.18em] text-white"><span className="grid size-8 place-items-center rounded-lg bg-blue-600 tracking-normal">L</span>LETRON</div><div className="px-4 py-6"><p className="mb-3 px-2 text-[10px] font-bold uppercase tracking-[0.18em] text-slate-500">Accounting</p><nav className="space-y-1" aria-label="Accounting navigation">{links.map(([label, href, Icon]) => <Link className="flex items-center gap-3 rounded-md px-3 py-2.5 text-sm text-slate-400 hover:bg-slate-900 hover:text-white" href={href} key={label}><Icon className="size-4" />{label}</Link>)}</nav></div><div className="absolute bottom-0 w-64 border-t border-slate-800 px-6 py-4 text-xs text-slate-500"><span className="block">Global Portal SSO</span><form action="/api/auth/logout" method="post"><button className="mt-2 text-slate-400 underline hover:text-white" type="submit">Đăng xuất</button></form></div></aside><div className="min-w-0 flex-1"><header className="flex h-16 items-center justify-between border-b bg-background px-5 md:px-8"><span className="text-sm font-semibold">Letron ERP</span><span className="rounded-full bg-muted px-3 py-1.5 text-xs text-muted-foreground">Accounting workspace</span></header>{children}</div></div>;
}
