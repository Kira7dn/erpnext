"use client";

import { FormEvent, useEffect, useState } from "react";

type Item = { name: string; item_code: string; description: string; qty: number; uom: string; rate?: number };
type Summary = { supplier: string; rfq: { name: string; status: string; items: Item[] }; quotation: { name: string; status: string } | null; purchase_order: { name: string; status: string; currency: string; items: Item[] } | null; purchase_receipt: { name: string; status: string } | null; payment_status: string; approval_status: string; deadline_at: string };

export default function SupplierPortalPage({ params }: { params: Promise<{ magicId: string }> }) {
  const [magicId, setMagicId] = useState(""); const [otp, setOtp] = useState(""); const [sent, setSent] = useState(false); const [summary, setSummary] = useState<Summary | null>(null); const [rates, setRates] = useState<Record<string, string>>({}); const [deliveryDate, setDeliveryDate] = useState(""); const [deliveryQty, setDeliveryQty] = useState<Record<string, string>>({}); const [xmlFile, setXmlFile] = useState<File | null>(null); const [message, setMessage] = useState("");
  useEffect(() => { void params.then(({ magicId: value }) => setMagicId(value)); }, [params]);
  async function refresh() { const response = await fetch("/api/supplier/session/process", { cache: "no-store" }); const payload = await response.json() as { data?: Summary }; if (response.ok) setSummary(payload.data ?? null); }
  async function requestOtp() { const response = await fetch(`/api/supplier/${encodeURIComponent(magicId)}/otp/request`, { method: "POST" }); setSent(response.ok); setMessage(response.ok ? "OTP đã được gửi tới email của Supplier." : "Magic Link không hợp lệ hoặc đã hết hạn."); }
  async function verify(event: FormEvent) { event.preventDefault(); const response = await fetch(`/api/supplier/${encodeURIComponent(magicId)}/otp/verify`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ otp }) }); if (!response.ok) { setMessage("OTP không đúng hoặc đã hết hạn."); return; } await refresh(); setMessage("Đã xác minh thành công."); }
  async function submitQuotation(event: FormEvent) { event.preventDefault(); const response = await fetch("/api/supplier/session/quotation", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ items: summary?.rfq.items.map((item) => ({ request_for_quotation_item: item.name, qty: item.qty, rate: Number(rates[item.name] ?? 0) })) }) }); setMessage(response.ok ? "Đã submit quotation và khóa quotation." : "Không thể submit quotation."); if (response.ok) await refresh(); }
  async function submitDelivery(event: FormEvent) { event.preventDefault(); const response = await fetch("/api/supplier/session/delivery", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ delivery_date: deliveryDate, idempotency_key: crypto.randomUUID(), items: (summary?.purchase_order?.items ?? []).map((item) => ({ purchase_order_item: item.name, delivered_qty: Number(deliveryQty[item.name] ?? 0), uom: item.uom })) }) }); setMessage(response.ok ? "Đã gửi delivery confirmation, đang chờ nội bộ kiểm tra." : "Không thể gửi delivery confirmation."); }
  async function submitXml(event: FormEvent) { event.preventDefault(); if (!xmlFile) return; const form = new FormData(); form.set("file", xmlFile); form.set("idempotency_key", crypto.randomUUID()); const response = await fetch("/api/supplier/session/xml-invoice", { method: "POST", body: form }); setMessage(response.ok ? "Đã nhận XML invoice, đang chờ nội bộ kiểm tra." : "Không thể upload XML invoice."); }
  async function logout() { await fetch("/api/supplier/session/logout", { method: "POST" }); setSummary(null); setMessage("Đã đăng xuất."); }
  if (!summary) return <main className="mx-auto max-w-lg space-y-6 p-6 md:p-12"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-violet-700">LeTRON Supplier Portal</p><h1 className="mt-3 text-3xl font-bold">Xác minh quotation</h1><p className="mt-2 text-muted-foreground">Không cần đăng nhập. Xác minh bằng OTP gửi tới email đã đăng ký.</p></div><button className="h-10 rounded-md bg-primary px-4 text-sm text-primary-foreground" disabled={!magicId} onClick={() => void requestOtp()}>Gửi Email OTP</button>{sent ? <form className="space-y-4 rounded-xl border bg-card p-5" onSubmit={(event) => void verify(event)}><label className="block space-y-2 text-sm font-medium" htmlFor="supplier-otp">Email OTP<input className="h-10 w-full rounded-md border bg-background px-3" id="supplier-otp" inputMode="numeric" maxLength={6} value={otp} onChange={(event) => setOtp(event.target.value)} /></label><button className="h-10 rounded-md bg-primary px-4 text-sm text-primary-foreground" type="submit">Xác minh và vào portal</button></form> : null}<p role="status">{message}</p></main>;
  return <main className="mx-auto max-w-3xl space-y-6 p-6 md:p-12"><header className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-violet-700">LeTRON Supplier Portal</p><h1 className="mt-3 text-3xl font-bold">Supplier workspace</h1><p className="mt-2 text-muted-foreground">Supplier: {summary.supplier}</p></div><button className="rounded-md border px-3 py-2 text-sm" onClick={() => void logout()}>Đăng xuất</button></header><section className="rounded-xl border bg-card p-5"><h2 className="font-semibold">Request for Quotation</h2><p className="mt-3 text-sm">RFQ: {summary.rfq.name} · Trạng thái: {summary.rfq.status} · Approval: {summary.approval_status} · Hạn: {summary.deadline_at}</p></section>{summary.quotation ? <p className="rounded-md bg-emerald-50 p-4 text-sm text-emerald-800">Quotation đã submit: {summary.quotation.name}. Đã khóa.</p> : <form className="space-y-4 rounded-xl border bg-card p-5" onSubmit={(event) => void submitQuotation(event)}><h2 className="font-semibold">Nhập báo giá</h2>{summary.rfq.items.map((item) => <div className="grid gap-2 sm:grid-cols-[1fr_10rem]" key={item.name}><label className="text-sm">{item.item_code} — {item.description || "Item"}<span className="block text-muted-foreground">Số lượng: {item.qty} {item.uom}</span></label><input className="h-10 rounded-md border bg-background px-3" min="0" step="0.01" required type="number" value={rates[item.name] ?? ""} onChange={(event) => setRates((current) => ({ ...current, [item.name]: event.target.value }))} /></div>)}<button className="h-10 rounded-md bg-primary px-4 text-sm text-primary-foreground" type="submit">Submit quotation</button></form>}{summary.purchase_order ? <><section className="space-y-4 rounded-xl border bg-card p-5"><h2 className="font-semibold">Purchase Order: {summary.purchase_order.name}</h2><p className="text-sm">{summary.purchase_order.status} · {summary.purchase_order.currency}</p><form className="space-y-3" onSubmit={(event) => void submitDelivery(event)}><h3 className="font-medium">Delivery confirmation</h3><input className="h-10 rounded-md border bg-background px-3" required type="date" value={deliveryDate} onChange={(event) => setDeliveryDate(event.target.value)} />{summary.purchase_order.items.map((item) => <div className="grid gap-2 sm:grid-cols-[1fr_10rem]" key={item.name}><label className="text-sm">{item.item_code} · đặt {item.qty} {item.uom}</label><input className="h-10 rounded-md border bg-background px-3" min="0" max={item.qty} step="0.01" type="number" value={deliveryQty[item.name] ?? ""} onChange={(event) => setDeliveryQty((current) => ({ ...current, [item.name]: event.target.value }))} /></div>)}<button className="h-10 rounded-md bg-primary px-4 text-sm text-primary-foreground" type="submit">Gửi xác nhận giao hàng</button></form></section><section className="space-y-4 rounded-xl border bg-card p-5"><h2 className="font-semibold">XML Invoice</h2><p className="text-sm text-muted-foreground">Receipt: {summary.purchase_receipt?.status ?? "Chờ kiểm tra"} · Payment: {summary.payment_status}</p><form className="flex flex-wrap gap-3" onSubmit={(event) => void submitXml(event)}><input accept=".xml,text/xml,application/xml" required type="file" onChange={(event) => setXmlFile(event.target.files?.[0] ?? null)} /><button className="h-10 rounded-md bg-primary px-4 text-sm text-primary-foreground" type="submit">Upload XML</button></form></section></> : null}<p role="status">{message}</p></main>;
}
