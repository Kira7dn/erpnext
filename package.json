{
  "name": "letron-erp-local",
  "private": true,
  "scripts": {
    "dev": "npx --yes concurrently --raw \"npm --prefix apps/auth-server run dev\" \"npm --prefix apps/erp run dev\"",
    "dev:auth": "npm --prefix apps/auth-server run dev",
    "dev:erp": "npm --prefix apps/erp run dev"
  }
}
